# ✅ Исправление: Поддержка переменной ADM

**Дата:** 17 января 2026  
**Проблема:** В Vercel переменная называется `ADM`, а код ищет `ADMIN_API_KEY`

---

## 🔍 Обнаруженная проблема

В Vercel Environment Variables переменная называется:
- `ADM` = `A246123b`

А код ищет:
- `process.env.ADMIN_API_KEY`
- `process.env.NEXT_PUBLIC_ADMIN_API_KEY`

**Результат:** Переменная может не находиться, если не переименовать в Vercel.

---

## ✅ Решение

Добавлена поддержка обоих вариантов в коде:

```typescript
const ADMIN_API_KEY = process.env.ADMIN_API_KEY || process.env.ADM || process.env.NEXT_PUBLIC_ADMIN_API_KEY || '';
```

Теперь код будет работать с:
- ✅ `ADMIN_API_KEY` (стандартное название)
- ✅ `ADM` (сокращение, как в Vercel)
- ✅ `NEXT_PUBLIC_ADMIN_API_KEY` (клиентский вариант)

---

## 📝 Обновленные файлы

1. ✅ `/app/api/admin/contest/participants/route.ts`
2. ✅ `/app/api/contest/active/route.ts`

---

## 🎯 Рекомендация

**Вариант 1 (Рекомендуется):** Переименовать в Vercel
- Переименовать `ADM` → `ADMIN_API_KEY` в Vercel Dashboard
- Это сделает код более понятным и стандартным

**Вариант 2:** Оставить как есть
- Код теперь поддерживает оба варианта
- Можно оставить `ADM` в Vercel

---

## ✅ Текущий статус

**Код обновлен:** ✅ Поддерживает оба варианта  
**Работает с текущей настройкой:** ✅ Да (с `ADM`)

**После деплоя:** Переменная `ADM` будет корректно использоваться!
