# 🚀 Инструкция по запуску

## Проблема: Сайт не запускается

### Решение:

1. **Установите зависимости:**
```bash
cd /Users/kelemetovmuhamed/Documents/vpnwebsite
npm install
```

2. **Запустите dev сервер:**
```bash
npm run dev
```

3. **Если порт 3000 занят:**
```bash
# Остановите процесс на порту 3000
kill $(lsof -ti:3000)

# Или используйте другой порт
npm run dev -- -p 3001
```

4. **Проверьте ошибки:**
```bash
# Проверка линтера
npm run lint

# Проверка TypeScript
npx tsc --noEmit
```

## Возможные проблемы:

### 1. Зависимости не установлены
**Решение:** `npm install`

### 2. Порт 3000 занят
**Решение:** 
```bash
kill $(lsof -ti:3000)
npm run dev
```

### 3. Ошибки TypeScript
**Решение:** Проверьте `tsconfig.json` и пути импортов

### 4. Отсутствует .env.local
**Решение:** Создайте файл `.env.local` с переменными окружения

## Быстрый запуск:

```bash
# 1. Установка зависимостей
npm install

# 2. Запуск сервера
npm run dev

# 3. Откройте в браузере
open http://localhost:3000
```

## Логи:

Если сервер не запускается, проверьте вывод команды `npm run dev` на наличие ошибок.

