# 🌐 Outlivion VPN Mini App

Telegram Mini App для управления VPN-подписками. Приложение построено на Next.js 16 с использованием React 19 и TypeScript.

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Требования](#-требования)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Разработка](#-разработка)
- [Тестирование](#-тестирование)
- [Сборка](#-сборка)
- [Деплой](#-деплой)
- [Архитектура](#-архитектура)
- [API документация](#-api-документация)
- [Структура проекта](#-структура-проекта)
- [Полезные ссылки](#-полезные-ссылки)

---

## 🚀 Быстрый старт

```bash
# Клонировать репозиторий
git clone <repository-url>
cd vpnwebsite

# Установить зависимости
npm install

# Создать файл .env.local (см. раздел Конфигурация)
cp .env.example .env.local

# Запустить dev сервер
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

---

## 📦 Требования

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0 (или yarn/pnpm)
- **Git**

---

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd vpnwebsite
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
# Базовый URL бэкенд API
NEXT_PUBLIC_API_BASE_URL=https://api.outlivion.space

# URL сервера подписок (Marzban)
NEXT_PUBLIC_SUBSCRIPTION_BASE_URL=https://vpn.outlivion.space

# Telegram Bot Token (для валидации initData на сервере)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# URL поддержки
NEXT_PUBLIC_SUPPORT_TELEGRAM_URL=https://t.me/outlivion_support

# URL помощи
NEXT_PUBLIC_HELP_BASE_URL=https://help.outlivion.space

# Username бота
NEXT_PUBLIC_BOT_USERNAME=outlivion_bot

# Sentry DSN (опционально, для мониторинга ошибок)
SENTRY_DSN=your_sentry_dsn_here
```

> **Примечание:** Файл `.env.local` не должен попадать в git (уже добавлен в `.gitignore`)

---

## ⚙️ Конфигурация

### Переменные окружения

#### Обязательные переменные

- `NEXT_PUBLIC_API_BASE_URL` - Базовый URL бэкенд API
- `NEXT_PUBLIC_SUBSCRIPTION_BASE_URL` - URL сервера подписок (Marzban)

#### Опциональные переменные

- `TELEGRAM_BOT_TOKEN` - Токен Telegram бота (для валидации initData)
- `NEXT_PUBLIC_SUPPORT_TELEGRAM_URL` - URL поддержки в Telegram
- `NEXT_PUBLIC_HELP_BASE_URL` - URL базы знаний
- `NEXT_PUBLIC_BOT_USERNAME` - Username бота
- `SENTRY_DSN` - DSN для Sentry (мониторинг ошибок)

### Конфигурация Next.js

Основная конфигурация находится в `next.config.ts`:

- **Оптимизация изображений** - автоматическая оптимизация через Next.js Image
- **CSP (Content Security Policy)** - настроена для безопасности
- **Sentry интеграция** - для мониторинга ошибок в production

---

## 💻 Разработка

### Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### Доступные команды

```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm start

# Линтинг кода
npm run lint

# Запуск тестов
npm test

# Запуск тестов с UI
npm run test:ui

# Запуск тестов с покрытием
npm run test:coverage
```

### Структура кода

```
vpnwebsite/
├── app/                    # Next.js App Router
│   ├── (auth)/            # Защищенные страницы
│   ├── api/               # API routes
│   └── layout.tsx         # Корневой layout
├── components/            # React компоненты
│   ├── blocks/           # Бизнес-компоненты
│   └── ui/              # UI компоненты
├── lib/                  # Утилиты и конфиг
│   ├── utils/           # Вспомогательные утилиты
│   └── __tests__/       # Тесты утилит
├── hooks/               # React хуки
├── store/              # Zustand сторы
├── types/              # TypeScript типы
└── public/            # Статические файлы
```

### Рекомендации по разработке

1. **Типизация** - Используйте TypeScript типы для всех компонентов и функций
2. **Компоненты** - Разделяйте на UI компоненты (`components/ui/`) и бизнес-компоненты (`components/blocks/`)
3. **API Routes** - Все API routes должны валидировать Telegram initData
4. **Обработка ошибок** - Используйте централизованный error handler (`lib/utils/errorHandler.ts`)
5. **Логирование** - Используйте `logError` из `lib/utils/logging.ts` для логирования ошибок

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Запуск всех тестов
npm test

# Запуск тестов в watch режиме
npm test -- --watch

# Запуск тестов с UI
npm run test:ui

# Запуск тестов с покрытием
npm run test:coverage
```

### Структура тестов

- **Компоненты** - `components/**/__tests__/*.test.tsx`
- **Утилиты** - `lib/__tests__/*.test.ts`
- **Test Utils** - `lib/test-utils.tsx` (утилиты для тестирования)

### Покрытие тестами

- ✅ UI компоненты: LoadingSpinner, SkeletonLoader
- ✅ Блоки компонентов: StatusCard, OfflineIndicator, CountdownTimer
- ✅ Утилиты: constants, telegram-validation, api-retry

---

## 🏗️ Сборка

### Production сборка

```bash
npm run build
```

Сборка создаст оптимизированную версию приложения в папке `.next/`.

### Оптимизации

- **Code Splitting** - автоматическое разделение кода
- **Image Optimization** - оптимизация изображений через Next.js Image
- **Tree Shaking** - удаление неиспользуемого кода
- **Minification** - минификация JavaScript и CSS

---

## 🚢 Деплой

### Vercel (рекомендуется)

1. Подключите репозиторий к Vercel
2. Настройте переменные окружения в Vercel Dashboard
3. Деплой произойдет автоматически при push в main ветку

### Ручной деплой

```bash
# Сборка
npm run build

# Запуск production сервера
npm start
```

### Переменные окружения для production

Убедитесь, что все переменные окружения настроены в вашем хостинге:

- `NEXT_PUBLIC_API_BASE_URL`
- `NEXT_PUBLIC_SUBSCRIPTION_BASE_URL`
- `TELEGRAM_BOT_TOKEN`
- `SENTRY_DSN` (опционально)

---

## 🏛️ Архитектура

### Основные технологии

- **Next.js 16** - React фреймворк с App Router
- **React 19** - UI библиотека
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Zustand** - управление состоянием
- **Vitest** - тестирование
- **Sentry** - мониторинг ошибок

### Архитектурные решения

1. **API Routes** - Все API запросы проксируются через Next.js API routes для валидации
2. **Централизованная валидация** - Валидация Telegram initData в `lib/utils/api-validation.ts`
3. **Централизованная обработка ошибок** - Error handler в `lib/utils/errorHandler.ts`
4. **Кэширование** - Стратегия кэширования в `lib/utils/cache-strategy.ts`
5. **Service Worker** - Офлайн-режим через Service Worker

---

## 📚 API документация

### 🏠 Центральная документация:
**→ [`Outlivion baza/docs/`](../docs/README.md)**
- [`03-flows/purchase-and-key-issue.md`](../docs/03-flows/purchase-and-key-issue.md) - Mini App purchase flow
- [`04-apis/vpnwebsite.md`](../docs/04-apis/vpnwebsite.md) - API usage guide (TODO)
- [`04-apis/vpn_api.md`](../docs/04-apis/vpn_api.md) - vpn_api endpoints (TODO)

### 📁 Локальная документация:
Полная документация API доступна в файле [API_DOCUMENTATION.md](./API_DOCUMENTATION.md) (TODO: создать).

### Основные endpoints (проксируются через Next.js):

- `GET /api/tariffs` - Получение тарифов
- `GET /api/user/status` - Статус пользователя
- `POST /api/orders/create` - Создание заказа
- `GET /api/payments/history` - История платежей
- `GET /api/referral/summary` - Сводка по реферальной программе

Все API endpoints требуют валидации Telegram initData (кроме публичных endpoints).

---

## 📁 Структура проекта

```
vpnwebsite/
├── app/                          # Next.js App Router
│   ├── (auth)/                  # Защищенные страницы
│   │   ├── page.tsx            # Главная страница
│   │   ├── profile/            # Профиль пользователя
│   │   ├── purchase/           # Покупка подписки
│   │   └── setup/              # Настройка VPN
│   ├── api/                    # API routes
│   │   ├── tariffs/           # Тарифы
│   │   ├── user/              # Пользователь
│   │   ├── orders/           # Заказы
│   │   └── referral/        # Реферальная программа
│   └── layout.tsx             # Корневой layout
├── components/                 # React компоненты
│   ├── blocks/                # Бизнес-компоненты
│   │   ├── StatusCard.tsx
│   │   ├── PaymentModal.tsx
│   │   └── ...
│   ├── ui/                   # UI компоненты
│   │   ├── LoadingSpinner.tsx
│   │   ├── SkeletonLoader.tsx
│   │   └── ...
│   └── ServiceWorkerProvider.tsx
├── lib/                      # Утилиты и конфиг
│   ├── api.ts               # API клиент
│   ├── config.ts           # Конфигурация
│   ├── utils/              # Вспомогательные утилиты
│   └── __tests__/          # Тесты утилит
├── hooks/                  # React хуки
│   ├── useTariffs.ts
│   ├── useSubscriptionUrl.ts
│   └── ...
├── store/                 # Zustand сторы
│   ├── user.store.ts
│   └── subscription.store.ts
├── types/                # TypeScript типы
├── public/              # Статические файлы
│   └── sw.js           # Service Worker
└── package.json
```

---

## 🔗 Полезные ссылки

- [Next.js документация](https://nextjs.org/docs)
- [React документация](https://react.dev)
- [TypeScript документация](https://www.typescriptlang.org/docs)
- [Tailwind CSS документация](https://tailwindcss.com/docs)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [API документация](./API_DOCUMENTATION.md)

---

## 📝 Лицензия

Приватный проект Outlivion.

---

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте [API документацию](./API_DOCUMENTATION.md)
2. Проверьте существующие [MD файлы](./) с описанием решений
3. Обратитесь в поддержку: [Telegram](https://t.me/outlivion_support)

---

*Последнее обновление: 2025-01-27*
