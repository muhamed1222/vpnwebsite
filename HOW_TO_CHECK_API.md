# 🔍 Как проверить Backend API

**Что это:** Команда для проверки доступности Backend API сервера

---

## 📝 Что такое `curl`?

`curl` — это команда для выполнения HTTP запросов из терминала. Она позволяет проверить, работает ли API сервер.

---

## 🎯 Что делает эта команда

```bash
curl https://api.outlivion.space/
```

**Что происходит:**
1. Отправляется GET запрос на корневой путь API сервера
2. Сервер должен вернуть статус работы API
3. Если сервер работает, вы увидите ответ

**Ожидаемый результат:**
```json
{"ok":true,"service":"outlivion-api"}
```

**✅ Проверено:** API сервер работает! Health check находится по корневому пути `/`

---

## 💻 Где вводить команду

### Вариант 1: Терминал на вашем компьютере (Mac/Linux)

1. **Откройте Terminal (Терминал):**
   - На Mac: `Cmd + Space` → введите "Terminal" → Enter
   - Или: Applications → Utilities → Terminal

2. **Введите команду:**
   ```bash
   curl https://api.outlivion.space/api/health
   ```

3. **Нажмите Enter**

4. **Результат:**
   - Если сервер работает: увидите JSON ответ
   - Если не работает: ошибка подключения

---

### Вариант 2: Через браузер (проще!)

**Просто откройте в браузере:**
```
https://api.outlivion.space/api/health
```

**Результат:**
- Если работает: увидите JSON: `{"ok":true,"service":"outlivion-api"}`
- Если не работает: ошибка 404 или таймаут

---

### Вариант 3: Через SSH на сервере

Если у вас есть доступ к серверу:

```bash
# 1. Подключиться к серверу
ssh root@72.56.93.135

# 2. Проверить API
curl https://api.outlivion.space/api/health

# 3. Или проверить локально
curl http://localhost:3000/api/health
```

---

## ✅ Что означает результат

### ✅ Успешный ответ:
```json
{"ok":true,"service":"outlivion-api"}
```
**Значит:** API сервер работает нормально! ✅

### ⚠️ Endpoint не найден (404):
```json
{"message":"Route GET:/api/health not found","error":"Not Found","statusCode":404}
```
**Значит:** API сервер работает, но endpoint `/api/health` не реализован
- Это нормально! Сервер доступен, просто health check endpoint может быть по другому пути
- Проверьте другие endpoints, которые точно должны работать

### ❌ Ошибка подключения:
```
curl: (7) Failed to connect to api.outlivion.space port 443
```
**Значит:** Сервер недоступен или не запущен

### ❌ Ошибка 404:
```json
{"error":"Not found"}
```
**Значит:** Сервер работает, но endpoint `/api/health` не найден

### ❌ Ошибка 500:
```json
{"error":"Internal Server Error"}
```
**Значит:** Сервер работает, но есть внутренняя ошибка

---

## 🔧 Что делать в зависимости от результата

### Если API работает ✅:
- Переходите к следующему шагу: проверка активного конкурса в БД

### Если API не работает ❌:

1. **Проверить статус сервера:**
   ```bash
   ssh root@72.56.93.135
   systemctl status outlivion-api
   ```

2. **Перезапустить сервис:**
   ```bash
   systemctl restart outlivion-api
   ```

3. **Проверить логи:**
   ```bash
   journalctl -u outlivion-api -n 50
   ```

---

## 🎯 Альтернативные способы проверки

### 1. Через браузер (самый простой):
```
https://api.outlivion.space/api/health
```

### 2. Через Postman/Insomnia:
- Создайте GET запрос
- URL: `https://api.outlivion.space/api/health`
- Отправьте запрос

### 3. Через JavaScript в консоли браузера:
```javascript
fetch('https://api.outlivion.space/api/health')
  .then(r => r.json())
  .then(console.log)
```

---

## 📊 Примеры результатов

### ✅ Работает:
```bash
$ curl https://api.outlivion.space/api/health
{"ok":true,"service":"outlivion-api"}
```

### ❌ Не работает:
```bash
$ curl https://api.outlivion.space/api/health
curl: (7) Failed to connect to api.outlivion.space port 443: Connection refused
```

---

## 🎯 Итог

**Самое простое:** Откройте в браузере:
```
https://api.outlivion.space/api/health
```

Если видите `{"ok":true,"service":"outlivion-api"}` — всё работает! ✅

---

**Нужна помощь с проверкой?** Скажите, какой результат вы получили!
