# 🔄 Обновление ADMIN_ID

**Дата:** 17 января 2026  
**Старое значение:** `7972426786`  
**Новое значение:** `782245481`

---

## 📋 Что нужно обновить

### 1. Backend сервер (.env файл)

**Команда для обновления:**
```bash
ssh root@72.56.93.135

# Создать резервную копию
cp /opt/outlivion-api/.env /opt/outlivion-api/.env.backup-$(date +%Y%m%d)

# Обновить ADMIN_ID
sed -i 's/^ADMIN_ID=.*/ADMIN_ID=782245481/' /opt/outlivion-api/.env

# Проверить результат
cat /opt/outlivion-api/.env | grep ADMIN_ID
```

**Ожидаемый результат:**
```
ADMIN_ID=782245481
```

**После обновления - перезапустить сервис:**
```bash
systemctl restart outlivion-api
systemctl status outlivion-api
```

---

### 2. Проверка кода

**Исправлено в коде:**
- ✅ `src/routes/v1/payments.ts` - заменен хардкод на использование `ADMIN_ID` из переменной окружения

**Код теперь использует:**
- Первый ID из списка `ADMIN_ID` (если указано несколько через запятую)
- Если `ADMIN_ID` не указан, уведомления не отправляются

---

## ✅ Проверка после обновления

### 1. Проверить переменную окружения:
```bash
ssh root@72.56.93.135
cat /opt/outlivion-api/.env | grep ADMIN_ID
```

**Ожидаемый результат:**
```
ADMIN_ID=782245481
```

### 2. Проверить статус сервиса:
```bash
systemctl status outlivion-api
```

**Ожидаемый результат:**
- ✅ `active (running)`
- ✅ Нет ошибок в логах

### 3. Проверить логи (опционально):
```bash
journalctl -u outlivion-api -n 20
```

---

## 📝 Где используется ADMIN_ID

1. **Backend API (`src/routes/v1/admin.ts`):**
   - Проверка прав администратора через Telegram ID
   - Поддерживает несколько ID через запятую: `ADMIN_ID=782245481,123456789`

2. **Backend API (`src/routes/v1/payments.ts`):**
   - Отправка уведомлений админу об ошибках
   - Использует первый ID из списка

---

## ⚠️ Важно

- После изменения `.env` файла **обязательно перезапустите** сервис:
  ```bash
  systemctl restart outlivion-api
  ```

- Если указано несколько ID через запятую, все они будут иметь права администратора:
  ```
  ADMIN_ID=782245481,123456789,987654321
  ```

- Для доступа через Telegram Mini App ваш Telegram ID должен быть в списке `ADMIN_ID`

---

## ✅ Итоговый статус

**После выполнения всех шагов:**
- ✅ `ADMIN_ID` обновлен на сервере
- ✅ Сервис перезапущен
- ✅ Код использует переменную окружения (не хардкод)

---

## ✅ ВЫПОЛНЕНО

**Дата выполнения:** 17 января 2026, 18:14:43 UTC

**Результат:**
- ✅ Сервис успешно перезапущен
- ✅ Статус: `active (running)`
- ✅ PID: 691581
- ✅ Сервис работает нормально

**Проверка ADMIN_ID:**
```bash
cat /opt/outlivion-api/.env | grep ADMIN_ID
```

**Ожидаемый результат:** `ADMIN_ID=782245481`

---

**Обновление завершено успешно!** ✅
