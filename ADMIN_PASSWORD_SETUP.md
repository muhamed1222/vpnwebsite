# 🔐 Настройка пароля для админ-панели

## Быстрая настройка

### Если фронтенд на Vercel:

1. Перейдите на https://vercel.com
2. Выберите проект `vpnwebsite` (или ваш проект)
3. Settings → Environment Variables
4. Добавьте переменную:
   - **Key:** `ADMIN_PASSWORD`
   - **Value:** ваш_секретный_пароль (например: `MySecretPassword123!`)
   - **Environment:** Production, Preview, Development (отметьте все)
5. Сохраните
6. Перезадеплойте проект (или подождите автоматического деплоя)

### Если фронтенд на вашем сервере:

```bash
# SSH на сервер фронтенда
ssh root@your-frontend-server

# Добавьте в .env файл
echo "ADMIN_PASSWORD=ваш_секретный_пароль" >> /path/to/vpnwebsite/.env

# Перезапустите Next.js (если запущен как сервис)
sudo systemctl restart vpnwebsite
# или
pm2 restart vpnwebsite
```

---

## 📝 Рекомендуемый пароль

**Примеры безопасных паролей:**
- `OutlivionAdmin2026!`
- `ContestPanel_Secret#123`
- Сгенерируйте случайный: `openssl rand -base64 32`

**Требования к паролю:**
- Минимум 8 символов
- Лучше: буквы + цифры + символы
- Не используйте простые пароли типа `123456` или `admin`

---

## ✅ Проверка настройки

После установки пароля:

1. Откройте `https://my.outlivion.space/admin/contest`
2. Должна появиться форма ввода пароля
3. Введите установленный пароль
4. Если пароль правильный - откроется админ-панель

---

## 🔒 Безопасность

- Пароль хранится только в переменных окружения
- Никогда не коммитьте пароль в git
- Используйте сложный пароль
- Меняйте пароль периодически

---

## 🆘 Если пароль не работает

1. Проверьте, что переменная окружения установлена:
   ```bash
   # На Vercel: Settings → Environment Variables
   # На сервере:
   cat .env | grep ADMIN_PASSWORD
   ```

2. Убедитесь, что проект перезадеплоен после добавления переменной

3. Проверьте имя переменной (должно быть точно `ADMIN_PASSWORD`)

4. Проверьте консоль браузера (F12) на наличие ошибок
