# 🔍 Отладка ошибки 401 в админ-панели

## Проблема

Получаете ошибку `401 (Unauthorized)` при загрузке участников конкурса.

## ✅ Что проверить

### 1. ADMIN_API_KEY в Vercel

**Проверьте, что переменная установлена:**

1. Откройте: https://vercel.com/dashboard
2. Проект `vpnwebsite` → Settings → Environment Variables
3. Убедитесь, что есть:
   - **Key:** `ADMIN_API_KEY` (БЕЗ префикса `NEXT_PUBLIC_`)
   - **Value:** `A246123b`
   - **Environments:** Production ✅ Preview ✅ Development ✅

**⚠️ Важно:** В Vercel для API routes (server-side) используйте переменную БЕЗ префикса `NEXT_PUBLIC_`.

### 2. Перезадеплойте проект

После добавления/изменения переменных окружения:
1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..." → Redeploy**
4. Подождите завершения деплоя (~1-2 минуты)

### 3. Проверьте консоль браузера

После деплоя откройте консоль (F12 → Console) и проверьте:
- Есть ли предупреждение `[Admin API] ADMIN_API_KEY not configured`?
- Если есть - переменная не читается правильно

### 4. Проверьте на сервере

На API сервере должно быть:
```bash
ADMIN_API_KEY=A246123b
```

**Проверка:**
```bash
ssh root@72.56.93.135
cat /opt/outlivion-api/.env | grep ADMIN_API_KEY
```

---

## 🔧 Возможные решения

### Решение 1: Используйте переменную без префикса

В Vercel:
- ✅ **Правильно:** `ADMIN_API_KEY=A246123b`
- ❌ **Неправильно:** `NEXT_PUBLIC_ADMIN_API_KEY=A246123b`

API routes в Next.js читают переменные БЕЗ префикса `NEXT_PUBLIC_`.

### Решение 2: Перезадеплойте после изменения переменных

**Важно:** После добавления/изменения переменных окружения в Vercel нужно перезадеплоить проект!

1. Settings → Environment Variables → Save
2. Deployments → ... → Redeploy

### Решение 3: Проверьте значение переменной

Убедитесь, что значение точное: `A246123b` (без пробелов, правильный регистр).

---

## 📋 Чек-лист

- [ ] `ADMIN_API_KEY` добавлен в Vercel (БЕЗ `NEXT_PUBLIC_`)
- [ ] Значение: `A246123b`
- [ ] Выбран Production + Preview + Development
- [ ] Проект перезадеплоен после добавления переменной
- [ ] На API сервере тоже установлен `ADMIN_API_KEY=A246123b`
- [ ] API сервер перезапущен

---

## 🆘 Если всё ещё не работает

1. **Проверьте логи Vercel:**
   - Deployments → выберите последний деплой → Logs
   - Ищите ошибки при сборке или запуске

2. **Проверьте логи API сервера:**
   ```bash
   ssh root@72.56.93.135
   journalctl -u outlivion-api -n 50 | grep -E "(Admin|401|Unauthorized)"
   ```

3. **Проверьте Network tab:**
   - F12 → Network
   - Найдите запрос к `/api/admin/contest/participants`
   - Проверьте заголовки запроса (должен быть `x-admin-api-key`)

---

## 🎯 После исправления

Когда всё настроено:
- Откройте `/admin/contest`
- Введите пароль: `A246123b`
- Данные должны загрузиться без ошибки 401
