# 🚀 Как запустить сайт

## Проблема: сайт не запускается

### Решение по шагам:

#### 1. Откройте терминал и перейдите в папку проекта:
```bash
cd /Users/kelemetovmuhamed/Documents/vpnwebsite
```

#### 2. Установите зависимости (если еще не установлены):
```bash
npm install
```

Это может занять несколько минут. Дождитесь завершения.

#### 3. Запустите dev сервер:
```bash
npm run dev
```

#### 4. Если порт 3000 занят:
```bash
# Остановите процесс на порту 3000
kill $(lsof -ti:3000)

# Затем запустите снова
npm run dev
```

#### 5. Откройте в браузере:
После запуска откройте: **http://localhost:3000**

## Альтернативный способ (через скрипт):

```bash
chmod +x start-server.sh
./start-server.sh
```

## Возможные ошибки и решения:

### ❌ "Cannot find module 'react'"
**Решение:** Запустите `npm install`

### ❌ "Port 3000 is already in use"
**Решение:** 
```bash
kill $(lsof -ti:3000)
npm run dev
```

### ❌ "Command not found: npm"
**Решение:** Установите Node.js с https://nodejs.org/

### ❌ Ошибки TypeScript
**Решение:** Проверьте, что все зависимости установлены:
```bash
npm install
npm run build
```

## Проверка что все работает:

1. ✅ Зависимости установлены: `ls node_modules | head -5`
2. ✅ Порт свободен: `lsof -ti:3000` (должно быть пусто)
3. ✅ Сервер запущен: откройте http://localhost:3000

## Если ничего не помогает:

1. Удалите `node_modules` и `package-lock.json`:
```bash
rm -rf node_modules package-lock.json
```

2. Переустановите зависимости:
```bash
npm install
```

3. Запустите снова:
```bash
npm run dev
```

## Контакты для помощи:

Если проблема не решается, проверьте:
- Версию Node.js: `node --version` (должна быть 18+)
- Версию npm: `npm --version`
- Логи ошибок в терминале при запуске `npm run dev`

