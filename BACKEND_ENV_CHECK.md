# 🔍 Результат проверки переменных окружения Backend

**Дата проверки:** 17 января 2026  
**Статус:** ⚠️ **ОБНАРУЖЕНА ПРОБЛЕМА**

---

## 📊 Результат проверки

**Команда:**
```bash
cat /opt/outlivion-api/.env | grep -E "TELEGRAM_BOT_TOKEN|ADMIN_ID|ADMIN_API_KEY"
```

**Результат:**
```
TELEGRAM_BOT_TOKEN=7948235015:AAGX6npuaEhZwYXWPY72Xag-d5FxTf2r6lw
ADMIN_API_KEY=bot_admin_key_super_secret_2024
ADMIN_ID=7972426786
ADMIN_API_KEY=A246123b
```

---

## ✅ Что правильно

1. **TELEGRAM_BOT_TOKEN:** ✅ Настроен
   - Значение: `7948235015:AAGX6npuaEhZwYXWPY72Xag-d5FxTf2r6lw`
   - ⚠️ Нужно проверить, совпадает ли с фронтендом (Vercel)

2. **ADMIN_ID:** ✅ Правильный
   - Значение: `7972426786`
   - Соответствует ожидаемому

3. **ADMIN_API_KEY (новый):** ✅ Правильный
   - Значение: `A246123b`
   - Совпадает с `ADM` на Vercel

---

## ⚠️ ПРОБЛЕМА: Дублирование ADMIN_API_KEY

В файле `.env` переменная `ADMIN_API_KEY` определена **ДВА РАЗА**:

1. **Старый ключ:** `ADMIN_API_KEY=bot_admin_key_super_secret_2024`
2. **Новый ключ:** `ADMIN_API_KEY=A246123b`

### Почему это проблема?

- В большинстве систем используется **последнее** значение
- Но это может вызвать путаницу
- Старый ключ не используется, но занимает место
- Рекомендуется удалить старую строку для чистоты

---

## 🔧 РЕШЕНИЕ: Удалить старый ADMIN_API_KEY

### Команда для исправления:

```bash
ssh root@72.56.93.135

# Создать резервную копию
cp /opt/outlivion-api/.env /opt/outlivion-api/.env.backup

# Удалить старую строку
sed -i '/^ADMIN_API_KEY=bot_admin_key_super_secret_2024$/d' /opt/outlivion-api/.env

# Проверить результат
cat /opt/outlivion-api/.env | grep -E "TELEGRAM_BOT_TOKEN|ADMIN_ID|ADMIN_API_KEY"
```

### Ожидаемый результат после исправления:

```
TELEGRAM_BOT_TOKEN=7948235015:AAGX6npuaEhZwYXWPY72Xag-d5FxTf2r6lw
ADMIN_ID=7972426786
ADMIN_API_KEY=A246123b
```

### После исправления - перезапустить сервис:

```bash
systemctl restart outlivion-api
systemctl status outlivion-api
```

---

## ⚠️ Дополнительная проверка: TELEGRAM_BOT_TOKEN

**Нужно проверить:** Совпадает ли `TELEGRAM_BOT_TOKEN` на Backend с фронтендом (Vercel)?

**На Backend:**
```
TELEGRAM_BOT_TOKEN=7948235015:AAGX6npuaEhZwYXWPY72Xag-d5FxTf2r6lw
```

**На Vercel (Frontend):**
- Проверить переменную `TELEGRAM_BOT_TOKEN` в настройках проекта
- Должна совпадать с Backend

---

## ✅ Итоговый статус

**Переменные окружения Backend:**
- ✅ `TELEGRAM_BOT_TOKEN` - настроен (нужно проверить совпадение с фронтендом)
- ✅ `ADMIN_ID` - правильный
- ✅ `ADMIN_API_KEY` - правильный (но есть дубликат старого ключа)
- ⚠️ **Требуется:** Удалить старую строку `ADMIN_API_KEY=bot_admin_key_super_secret_2024`

---

**Статус:** ⚠️ Требуется удаление старого `ADMIN_API_KEY` для чистоты конфигурации
