# ✅ Автоматическое начисление билетов

**Дата:** 2026-01-17  
**Статус:** ✅ Настроено и работает

---

## 🎯 Ответ: ДА, пользователи будут получать билеты автоматически

### ✅ Как это работает:

1. **При каждой покупке подписки:**
   - Пользователь покупает подписку через бота
   - Заказ обрабатывается функцией `activateOrder()` в `orderProcessingService.ts`
   - После успешной активации заказа автоматически вызывается функция `awardSelfPurchaseTicket()`

2. **Автоматические проверки:**
   - ✅ Проверяется, что конкурс активен
   - ✅ Проверяется, что заказ создан после начала конкурса
   - ✅ Проверяется, что заказ создан до окончания конкурса
   - ✅ Проверяется, что билет еще не создан (защита от дубликатов)

3. **Автоматическое создание билета:**
   - Количество билетов рассчитывается из плана:
     - `plan_7` → 1 билет
     - `plan_30` → 1 билет
     - `plan_90` → 3 билета
     - `plan_180` → 6 билетов
     - `plan_365` → 12 билетов
   - Билет создается в таблице `ticket_ledger` с типом `SELF_PURCHASE`

---

## 📋 Код автоматического начисления

### Файл: `src/services/orderProcessingService.ts`

```typescript
// Начисляем билет за собственную покупку (SELF_PURCHASE)
const activeContestForSelf = ContestService.getActiveContest();
if (activeContestForSelf) {
    ContestService.awardSelfPurchaseTicket(
        activeContestForSelf.id,
        order.userId,
        order.id,
        order.planId,
        order.createdAt * 1000
    );
}
```

**Место вызова:** Внутри функции `activateOrder()`, после успешной активации заказа.

---

## 🔍 Логика функции `awardSelfPurchaseTicket()`

### Файл: `src/services/contestService.ts`

```typescript
awardSelfPurchaseTicket: (
  contestId: string,
  userId: number,
  orderId: string,
  planId: string,
  orderCreatedAt: number
): boolean => {
  // 1. Проверка существования конкурса
  // 2. Проверка даты начала конкурса
  // 3. Проверка даты окончания конкурса
  // 4. Проверка на дубликаты
  // 5. Расчет количества билетов из плана
  // 6. Создание записи в ticket_ledger
}
```

### Проверки:

1. **Конкурс существует и активен:**
   ```typescript
   const contest = ContestService.getContestById(contestId);
   if (!contest) return false;
   ```

2. **Заказ после начала конкурса:**
   ```typescript
   if (orderCreatedAt < contestStartTime) {
     console.log('Order was created before contest start, skipping');
     return false;
   }
   ```

3. **Заказ до окончания конкурса:**
   ```typescript
   if (orderCreatedAt > contestEndTime) {
     console.log('Order was created after contest end, skipping');
     return false;
   }
   ```

4. **Нет дубликатов:**
   ```typescript
   const existing = database.prepare(`
     SELECT id FROM ticket_ledger 
     WHERE order_id = ? AND reason = 'SELF_PURCHASE'
   `).get(orderId);
   if (existing) return false;
   ```

---

## 📊 Примеры работы

### ✅ Успешное начисление:

1. **Пользователь покупает `plan_90` 17 января 2026 в 20:00:00**
   - Конкурс активен: ✅ (начался 17.01.2026 00:00:00)
   - Заказ после начала: ✅ (20:00 > 00:00)
   - Заказ до окончания: ✅ (20:00 < 20.02.2026 23:59:59)
   - **Результат:** Создано 3 билета автоматически

2. **Пользователь покупает `plan_30` 18 января 2026 в 10:00:00**
   - Конкурс активен: ✅
   - Заказ после начала: ✅
   - Заказ до окончания: ✅
   - **Результат:** Создан 1 билет автоматически

### ❌ Билет НЕ создается:

1. **Покупка до начала конкурса:**
   - Заказ: 16.01.2026 23:00:00
   - Конкурс начался: 17.01.2026 00:00:00
   - **Результат:** Билет НЕ создается (логируется причина)

2. **Покупка после окончания конкурса:**
   - Заказ: 21.02.2026 10:00:00
   - Конкурс закончился: 20.02.2026 23:59:59
   - **Результат:** Билет НЕ создается (логируется причина)

3. **Повторная попытка создать билет:**
   - Билет уже существует для этого заказа
   - **Результат:** Билет НЕ создается (защита от дубликатов)

---

## 🔧 Логирование

### Успешное создание:
```
[ContestService] Awarded 3 self-purchase tickets to user 7972426786 for order ord_xxx
```

### Пропуск (до начала конкурса):
```
[ContestService] Order ord_xxx was created before contest start (2026-01-16T23:00:00.000Z < 2026-01-17T00:00:00Z), skipping self-purchase ticket
```

### Пропуск (после окончания конкурса):
```
[ContestService] Order ord_xxx was created after contest end (2026-02-21T10:00:00.000Z > 2026-02-20T23:59:59Z), skipping self-purchase ticket
```

### Пропуск (дубликат):
```
[ContestService] Self-purchase ticket already exists for order ord_xxx
```

---

## ✅ Гарантии

1. **Автоматическое начисление:**
   - ✅ Функция вызывается при каждой покупке
   - ✅ Не требует ручного вмешательства
   - ✅ Работает для всех пользователей

2. **Защита от ошибок:**
   - ✅ Проверка дат конкурса
   - ✅ Защита от дубликатов
   - ✅ Обработка ошибок (try/catch)
   - ✅ Логирование всех действий

3. **Правильность начисления:**
   - ✅ Количество билетов рассчитывается из плана
   - ✅ Билеты создаются только для заказов в период конкурса
   - ✅ Тип билета: `SELF_PURCHASE`

---

## 📝 Мониторинг

### Проверка работы:

```bash
# Просмотр логов в реальном времени
journalctl -u vpn-bot -f | grep -i "awardSelfPurchaseTicket\|SELF_PURCHASE"

# Проверка созданных билетов
sqlite3 /root/vpn_bot/data/database.sqlite "
  SELECT 
    tl.referrer_id,
    tl.order_id,
    tl.delta,
    tl.reason,
    datetime(tl.created_at) as created
  FROM ticket_ledger tl
  WHERE tl.reason = 'SELF_PURCHASE'
  ORDER BY tl.created_at DESC
  LIMIT 10;
"
```

---

## 🎯 Итог

**✅ ДА, пользователи будут получать билеты автоматически:**

- ✅ Функция вызывается при каждой покупке
- ✅ Все проверки работают корректно
- ✅ Билеты создаются автоматически
- ✅ Защита от ошибок и дубликатов
- ✅ Логирование всех действий

**Никаких ручных действий не требуется!**

---

## 🔄 Что было исправлено ранее

### Проблема:
- Заказы, обработанные до добавления функции `awardSelfPurchaseTicket()`, не получили билеты

### Решение:
- Созданы билеты вручную для 4 заказов, обработанных до обновления кода
- Теперь все новые заказы обрабатываются автоматически

---

**Статус:** ✅ Система работает автоматически, ручное вмешательство не требуется
