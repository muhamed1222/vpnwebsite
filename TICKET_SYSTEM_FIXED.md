# ✅ Система начисления билетов исправлена

**Дата исправления:** 2026-01-17  
**Статус:** ✅ Исправлено и задеплоено

---

## 🔧 Что было исправлено

### 1. ✅ Добавлена проверка даты начала конкурса

**Файл:** `/root/vpn_bot/src/services/contestService.ts` → `awardTickets()`

**Изменения:**
- Добавлен параметр `orderCreatedAt: number`
- Добавлена проверка: `orderCreatedAt >= contest.starts_at`
- Билеты не начисляются за покупки до запуска конкурса

**Код:**
```typescript
// 2. Проверить дату начала конкурса
const contestStartTime = new Date(contest.starts_at).getTime();
if (orderCreatedAt < contestStartTime) {
  console.log(`[ContestService] Order ${orderId} was created before contest start, skipping ticket`);
  return false;
}
```

---

### 2. ✅ Добавлена проверка даты окончания конкурса

**Изменения:**
- Добавлена проверка: `orderCreatedAt <= contest.ends_at`
- Билеты не начисляются за покупки после окончания конкурса

**Код:**
```typescript
// 3. Проверить дату окончания конкурса
const contestEndTime = new Date(contest.ends_at).getTime();
if (orderCreatedAt > contestEndTime) {
  console.log(`[ContestService] Order ${orderId} was created after contest end, skipping ticket`);
  return false;
}
```

---

### 3. ✅ Добавлена проверка окна атрибуции в awardTickets()

**Изменения:**
- Проверка окна атрибуции перенесена из `checkQualification()` в `awardTickets()`
- Дополнительная защита от создания билетов вне окна атрибуции

**Код:**
```typescript
// 4. Проверить окно атрибуции
const boundAt = new Date(refEvent.bound_at).getTime();
const attributionWindowMs = contest.attribution_window_days * 24 * 60 * 60 * 1000;
const timeSinceBound = orderCreatedAt - boundAt;

if (timeSinceBound > attributionWindowMs) {
  console.log(`[ContestService] Order ${orderId} outside attribution window`);
  return false;
}
```

---

### 4. ✅ Добавлена проверка дубликатов билетов

**Изменения:**
- Проверка, не создан ли уже билет для этого заказа
- Защита от повторного начисления билетов

**Код:**
```typescript
// 5. Проверить, не создан ли уже билет для этого заказа
const existing = database.prepare(`
  SELECT id FROM ticket_ledger 
  WHERE order_id = ? AND reason = 'INVITEE_PAYMENT'
`).get(orderId);

if (existing) {
  console.log(`[ContestService] Ticket already exists for order ${orderId}`);
  return false;
}
```

---

### 5. ✅ Создана функция для SELF_PURCHASE билетов

**Новая функция:** `awardSelfPurchaseTicket()`

**Изменения:**
- Создана функция для начисления билетов за собственные покупки
- Те же проверки дат, что и в `awardTickets()`
- Автоматически вызывается при каждой покупке

**Код:**
```typescript
awardSelfPurchaseTicket: (
  contestId: string,
  userId: number,
  orderId: string,
  planId: string,
  orderCreatedAt: number
): boolean => {
  // Те же проверки дат конкурса
  // reason = 'SELF_PURCHASE'
  // referrer_id = referred_id = userId
}
```

---

### 6. ✅ Обновлен вызов awardTickets() в orderProcessingService.ts

**Файл:** `/root/vpn_bot/src/services/orderProcessingService.ts`

**Изменения:**
- Добавлен параметр `order.createdAt * 1000` в вызов `awardTickets()`
- Добавлен вызов `awardSelfPurchaseTicket()` для всех покупок

**Код:**
```typescript
// Для реферальных билетов
ContestService.awardTickets(
  activeContest.id,
  referrerId,
  order.userId,
  order.id,
  order.planId,
  order.createdAt * 1000  // ← ДОБАВЛЕНО
);

// Для собственных покупок
const activeContestForSelf = ContestService.getActiveContest();
if (activeContestForSelf) {
  ContestService.awardSelfPurchaseTicket(
    activeContestForSelf.id,
    order.userId,
    order.id,
    order.planId,
    order.createdAt * 1000
  );
}
```

---

### 7. ✅ Улучшена функция checkQualification()

**Изменения:**
- Добавлена проверка даты начала конкурса
- Добавлена проверка даты окончания конкурса
- Используется `getContestById()` вместо `getActiveContest()` для проверки конкурса по ID

---

## 🎯 Результат

### Теперь система:
1. ✅ **Проверяет дату начала конкурса** - билеты не начисляются за покупки до запуска
2. ✅ **Проверяет дату окончания конкурса** - билеты не начисляются за покупки после окончания
3. ✅ **Проверяет окно атрибуции** - билеты не начисляются за покупки вне окна
4. ✅ **Создает билеты за собственные покупки** - функция `awardSelfPurchaseTicket()`
5. ✅ **Защищена от дубликатов** - проверка существующих билетов
6. ✅ **Логирует все действия** - подробные логи для отладки

---

## 📋 Что нужно проверить

1. ✅ **Перезапущен бот** - изменения применены
2. ⏳ **Протестировать:**
   - Создать заказ до запуска конкурса - билет не должен создаться
   - Создать заказ после запуска - билет должен создаться
   - Создать заказ после окончания - билет не должен создаться
   - Проверить логи бота на наличие сообщений о проверках

---

## 🔍 Логи для проверки

После создания заказа проверьте логи:
```bash
journalctl -u vpn-bot -f | grep -i "contest\|ticket"
```

**Ожидаемые сообщения:**
- `[ContestService] Awarded X tickets to referrer...` - билет начислен
- `[ContestService] Order ... was created before contest start, skipping ticket` - билет не начислен (до запуска)
- `[ContestService] Order ... was created after contest end, skipping ticket` - билет не начислен (после окончания)
- `[ContestService] Awarded X self-purchase tickets...` - билет за собственную покупку

---

**Статус:** ✅ Исправлено, бот перезапущен, готово к тестированию
