# 🔧 Исправление проблемы: бот не отвечает

**Дата:** 2026-01-17  
**Проблема:** Бот не отвечает на сообщения  
**Статус:** В процессе диагностики

---

## 🔍 Диагностика

### Проблема:
- Webhook возвращает `502 Bad Gateway`
- Бот запущен (`systemctl status` показывает `active (running)`)
- Но порт 3000 не слушается
- Webhook показывает ошибку: `Wrong response from the webhook: 502 Bad Gateway`

### Причина:
Бот падает сразу после запуска или не может слушать на порту 3000.

---

## ✅ Что было проверено

1. ✅ **Статус бота:** `active (running)` - бот запущен
2. ✅ **Процесс:** Процесс существует
3. ❌ **Порт 3000:** Не слушается
4. ❌ **Webhook:** Возвращает 502 Bad Gateway
5. ✅ **Код:** Синтаксически правильный

---

## 🔧 Решение

### Шаг 1: Проверить, почему бот не слушает порт

Бот должен слушать на `127.0.0.1:3000`, но порт не открыт. Возможные причины:
- Бот падает сразу после запуска
- Ошибка при инициализации
- Проблема с базой данных

### Шаг 2: Проверить логи при запуске

```bash
journalctl -u vpn-bot -f
```

### Шаг 3: Запустить бот вручную для диагностики

```bash
cd /root/vpn_bot
node_modules/.bin/tsx server.ts
```

---

## 📝 Следующие шаги

1. Проверить логи бота на наличие ошибок
2. Убедиться, что бот слушает на порту 3000
3. Проверить, что nginx может подключиться к боту
4. Проверить webhook после исправления

---

**Статус:** Требуется дополнительная диагностика
